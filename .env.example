# Environment Configuration
# Copy this file to .env and update values for your environment

# Server Configuration
PORT=3000
NODE_ENV=production

# Domain Configuration (without www prefix)
# Used for SEO redirects and sitemap generation
DOMAIN=convertaudiofast.com

# Examples for different environments:
# Production: DOMAIN=convertaudiofast.com
# Staging: DOMAIN=staging.convertaudiofast.com
# Development: Leave blank or use localhost

# Security Notes:
# - In production, set NODE_ENV=production to enable security features
# - In development, use NODE_ENV=development to disable WWW redirect
